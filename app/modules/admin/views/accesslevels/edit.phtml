<?php
$accesslevel = $this->accesslevel;


$this->layout()->addGlobalJavascript();
$this->layout()->addJavascript($this->router()->makeAbsoluteUri('/js/jquery-ui-1.8.17.custom.min.js'));
$this->layout()->addStylesheet($this->router()->makeAbsoluteUri('/css/jquery-ui/jquery-ui-1.8.17.custom.css'));

$this->layout()->appendSection('layout.head', '
<script type="text/javascript">

	$(document).ready(function() {

		var del_button = $("<div style=\"margin-bottom: 1em;\"><a href=\"#\">Delete Access Level</a></div>");
		del_button.click(function () {
			$(\'#dialog-access\').html(\'' . $accesslevel->name . '\');
			$(\'#dialog\').dialog(\'open\');
		});
		$("#delete_button_container").append(del_button);

		$( "#dialog:ui-dialog" ).dialog( "destroy" );
		$(\'#dialog\').dialog({
            modal: true,
            autoOpen: false,
            width: 400,
            modal: true,
            resizable: false,
            buttons: {
                "Delete": function() {
                	$(\'#submitdelete\').val(\'1\');
                    $(\'#accesslevelform\').submit();
                },
                "Cancel": function() {
                    $(this).dialog("close");
                }
            }
        });
	});

</script>
');
?>



<h1 class="green-bar">Edit Access Level: <?php $this->out($accesslevel->name); ?></h1>
<p>Here you can edit this access level.</p>

<div id="delete_button_container"></div>


<?php
Ecl_Helper_Html::form('accesslevelform', $this->request()->uri());
Ecl_Helper_Html::formHidden('session_key', $this->model('user')->getSessionKey());
Ecl_Helper_Html::formHidden('submitdelete', '');
?>

<dl class="form">
	<dt>Access Level Name</dt>
	<dd><?php Ecl_Helper_Html::formInput('name', 50, 250, $accesslevel->name); ?></dd>
</dl>

<div>
	<?php Ecl_Helper_Html::formSubmit('submitsave', 'Save Changes'); ?>
</div>




<?php
Ecl_Helper_Html::formClose();
?>

<div id="dialog" title="Verify Delete Access Level">
	<p><span class="ui-icon ui-icon-alert" style="float:left; margin:0 7px 0 0;"></span>Please confirm that this access level will be deleted.</p><p>Any Items associated with this access level will remain, but no longer be registered with the access level.</p><p>This operation cannot be undone.</p>
	<p id="dialog-access"></p>
</div>

